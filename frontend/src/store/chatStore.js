/**
 * Chat state initializer (Phase 18 — JSX conversion).
 */

// Clean initial state — no fake welcome message
export const initialChatState = {
  messages: [],
  isLoading: false,
  activeReasoningSteps: [],
  selectedLanguage: 'en',
};
