/**
 * Chat type definitions — JavaScript version.
 * Types are implicit in JS; this file exists for import compatibility.
 * Refer to backend app/schemas/query.py and app/schemas/spatial.py for canonical contracts.
 */
export {};
